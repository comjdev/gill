<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  {% for post in posts %}
  <url>
    <loc>{{ post.permalink | uriencode }}</loc>
    {% set mapped = config.sitemap.listing_lastmods %}
    {% set lastmod = (mapped and mapped[post.permalink]) or post.updated or post.date %}
    {% if lastmod %}
    <lastmod>{{ lastmod | formatDate }}</lastmod>
    {% endif %}
    <changefreq>monthly</changefreq>
    <priority>0.6</priority>
  </url>
  {% endfor %}

  {# Homepage lastmod is the latest work/blog/homepage content change, not build time #}
  <url>
    <loc>{{ config.url | uriencode }}</loc>
    {% if config.sitemap.homepage_lastmod %}
    <lastmod>{{ config.sitemap.homepage_lastmod | formatDate }}</lastmod>
    {% endif %}
    <changefreq>daily</changefreq>
    <priority>1.0</priority>
  </url>

  {% for tag in tags %}
  <url>
    <loc>{{ tag.permalink | uriencode }}</loc>
    <changefreq>weekly</changefreq>
    <priority>0.2</priority>
  </url>
  {% endfor %}

  {% for cat in categories %}
  <url>
    <loc>{{ cat.permalink | uriencode }}</loc>
    <changefreq>weekly</changefreq>
    <priority>0.2</priority>
  </url>
  {% endfor %}
</urlset>
